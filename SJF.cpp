#include<processor1>
#include<processor2>
#include<processor3>
#include<processor4>
#include<iostream>
#include<queue>

void SFJ(int mem1, int mem2, int mem3, int mem4, int speed1, int speed2, int speed3, int speed4, queue<long long> processes) {
  //sort the queue of processes from shortest time to most time
  
  //check if any of the processors are full
  
  //if not, the pop the next process in the queue onto the processor
}
